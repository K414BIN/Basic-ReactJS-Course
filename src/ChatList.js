const ChatList = ({ chatArray }) => {
    return (
                <div className="chatWrapper">
                {/* to be continued ...*/}
                </div>
           );
}
export default ChatList;